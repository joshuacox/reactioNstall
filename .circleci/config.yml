version: 2

jobs:
  build:
    machine: true
    environment:
      MINIKUBE_OPTS: "--vm-driver=none"
    steps:
      - checkout

      - run: sudo bash .ci/linux_before_install.sh

      - run: rm -f .bash_profile .bashrc .zshrc .profile

      - run: bash rxNstall
